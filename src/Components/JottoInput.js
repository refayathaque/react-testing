import React from 'react';
import { connect } from 'react-redux';

const JottoInput = () => {
  return (
    <div>
      Input
    </div>
  )
};

const mapStateToProps = (state) => {
  return {

  }
}

export default connect(mapStateToProps)(JottoInput);
